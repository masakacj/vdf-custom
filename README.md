# VDF Custom

Private customization workspace based on [0x90d/videoduplicatefinder](https://github.com/0x90d/videoduplicatefinder).

The repository is bootstrapped from the upstream `master` branch and keeps an `upstream-master` mirror branch for future synchronization.
